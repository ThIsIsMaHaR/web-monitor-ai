{
  "name": "web-monitor-ai",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "build": "if [ -d \"client\" ]; then npm install --prefix client && npm run build --prefix client; elif [ -d \"frontend\" ]; then npm install --prefix frontend && npm run build --prefix frontend; else npm install && npm run build; fi"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "axios": "^1.13.5",
    "cheerio": "^1.2.0",
    "cors": "^2.8.6",
    "diff": "^8.0.3",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "mongoose": "^9.2.2",
    "openai": "^6.24.0"
  }
}