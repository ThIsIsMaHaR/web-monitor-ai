{
  "name": "web-monitor-ai-root",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server/server.js || node server.js",
    "build": "npm install && (npm install --prefix server && npm run build --prefix server) || (npm install --prefix client && npm run build --prefix client) || npm run build"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "axios": "^1.13.5",
    "cheerio": "^1.2.0",
    "cors": "^2.8.6",
    "diff": "^8.0.3",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "mongoose": "^9.2.2",
    "openai": "^6.24.0"
  }
}